import React from 'react'

// If something goes wrong, direct to /error
// Also make a component that shows an error perhaps?
const ErrorScreen = () => {
	return <div></div>
}

export default ErrorScreen
